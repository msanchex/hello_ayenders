# frozen_string_literal: true

RSpec.describe HelloAyenders do
  it "has a version number" do
    expect(HelloAyenders::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
